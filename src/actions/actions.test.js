import moxios from 'moxios';



describe('getSecretWord action creater', () => {

    beforeEach(() => {
        moxios.install();
    });

    afterEach(() => {
        moxios.uninstall();
    });


});